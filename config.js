const MindsDB = require("mindsdb-js-sdk").default;
await MindsDB.connect({
  host: 'https://cloud.mindsdb.com', // This is the default host, so you can leave it blank.
  username: 'alissatroianonyc@gmail.com',
  password: 'password'
});